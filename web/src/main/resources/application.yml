server:
  servlet:
    context-path: /rappidu

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rappidu
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update